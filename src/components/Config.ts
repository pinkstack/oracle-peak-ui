export class Config {
    static MAX_MEASUREMENTS_SIZE = 200;
    static MAX_EVENTS_SIZE = 20;
    static MQTT_BROKER_URL = "ws://broker.hivemq.com:8000/mqtt";
    static DEFAULT_LIVE_VIEW_TAB = 'feed' // or list or events;
}
